import styled from 'styled-components';

export default styled.header`
  padding: 1rem 5rem;

  img {
    height: 5rem;
    margin-right: 2.5rem;
  }

  .desktop-nav {
    
  }

  .mobile-nav {
    display: none;
  }
`;